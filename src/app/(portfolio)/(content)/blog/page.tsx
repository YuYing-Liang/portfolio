import { Container, Title } from "@mantine/core";
import { type NextPage } from "next";

const Blog: NextPage = () => {
  return (
    <Container>
      <Title order={1}>Blog</Title>
    </Container>
  );
};

export default Blog;
